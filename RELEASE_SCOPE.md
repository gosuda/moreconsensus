# Release Scope Lock

This file is the self-contained release-scope lock for the active EPaxos production-readiness goal.

## Scope rule

A release claim is allowed only when the item is listed in **Closed release items** with current evidence. If any item needed for the requested production-readiness goal is listed in **Open release items**, the release decision is no-go. New evidence must be added here before changing the decision.

## Current release decision

No-go.

The implemented EPaxos core, explicit TOQ core mode, deterministic timing subset, HTTP KV service-hardening changes, local fault campaign, local mixed-version binary drill, operations artifacts, local Go runner, evidence bundle, and go/no-go workflow are ready for reviewer assessment under a simulation/local-loopback release scope. The broader production-readiness request is not complete because broader formal proof, target-environment deployment and data-lifecycle drills, target capacity measurements, and incident-readiness exercises remain open below. External multi-host Jepsen histories are outside the current release evidence.

## Fault-tolerance target and evidence matrix

This matrix is the current simulation/local-loopback fault-tolerance envelope. A row is a closed claim only inside its stated **Fault-count boundary** and only when a **Healthy quorum** remains where the row requires quorum progress. "Normal operation" means no safety violation, no duplicate committed application, and the row-specific availability or fail-closed behavior. These rows do not change the `No-go.` decision because broader proof, target-environment deployment and data-lifecycle drills, target capacity evidence, and incident readiness remain open below.

| Fault-tolerance target | Fault-count boundary | Normal operation maintained when | Evidence proving normal operation | Non-claims / open boundary |
| --- | --- | --- | --- | --- |
| Quorum-preserving crash, restart, owner stop, and omission/storage-unavailable tolerance | Supported one-to-seven-voter configurations; progress requires a slow quorum/Healthy quorum with tolerated unavailable counts `0,0,1,1,2,2,3` for `N=1..7`; no Byzantine behavior. | A healthy quorum can continue or recover unfinished instances; no-quorum sides emit no unsafe application output; restarted, omitted, or storage-failed replicas catch up without duplicate application after heal/restore. | `go test ./epaxos -run 'TestDST.*(FailureBoundary|StorageFailureBoundary|Linearizability|Storage)|TestDeterministic.*(Linearizability|Liveness|Availability)|TestClusterSizesOneThroughSevenCommit' -count=1`; `TestDSTFailureBoundarySlowQuorumSizesOneThroughSeven`; `TestDSTStorageFailureBoundarySlowQuorumSizesOneThroughSeven`; `TestDSTStorageFailureRetriesOutstandingReadyExactlyOnceWithHealthyQuorum`; `go test ./epaxos`; `bash tests/chaos_fault_campaign.sh`; `tla/EPaxosRecovery.cfg`; `tla/EPaxosRecoveryFive.cfg`. | Does not prove remote multi-host Jepsen behavior, arbitrary membership-change recovery, Byzantine tolerance, or progress without a durable slow quorum. |
| Network delivery faults and partitions | Duplicate, reordered, dropped, and locally partitioned messages inside deterministic simulator/local Jepsen profiles; progress requires a Healthy quorum side. | Majority/quorum side preserves command order and liveness; minority side withholds output; healed replicas converge without duplicate committed commands. | `epaxos/dst_test.go`; `epaxos/stress_test.go`; `bash tests/chaos_fault_campaign.sh`; local Jepsen restart/transport/storage profiles; `tla/KVOmissionRecovery.cfg`. | No target-environment remote claim; external multi-host Jepsen histories are outside current simulation-scoped release evidence. |
| Deterministic timing faults | Logical RawNode tick skew/pause and explicit numeric record timestamps; no core-owned OS wall-clock reads; explicit TOQ relies on caller-supplied clock discipline, with every `TOQOneWayDelay` value including network delay plus clock-skew margin. | Skewed/paused replicas do not duplicate commands or violate dependency order; healthy quorum members make permitted progress; resumed replicas catch up. | `go test ./epaxos -run 'TestUnevenLogicalTickSkewAndBurstConvergesWithoutDuplicates|TestPausedClockDoesNotTickOrProcessReadyUntilResume' -count=1`; `bash tests/chaos_fault_campaign.sh`; `tla/TOQClockDiscipline.cfg`. | OS wall-clock mutation, clock synchronization, one-way-delay measurement, and operational TOQ clock discipline remain outside current simulation-scoped release evidence. |
| VM rollback and restart catch-up | One replica rolled back to an earlier durable snapshot while isolated, then healed with quorum data still trusted; finite TLA rollback-allocation coverage covers one older checkpoint plus one later own committed instance learned back from quorum. | The rolled-back replica catches up from quorum state, advances local `nextInstance` past learned own refs, allocates a fresh local instance after catch-up, and does not reapply already delivered commands. | `go test ./epaxos -run TestRolledBackNodeCatchesUpFromQuorumWithoutDuplicateApply -count=1`; `go test ./epaxos -run TestSimulatorRestoredLocalStorageAdvancesPastLearnedLocalCommit -count=1`; `tla/EPaxosRollbackAllocation.cfg`; `bash tests/chaos_fault_campaign.sh`; `bash tests/tla_model_check.sh`. | Does not close in-place record-level corruption repair, synthesized reconstruction without a verified checkpoint, multi-replica/quorum loss, or unbounded rollback histories. |
| Storage unavailable and destructive-storage recovery | Local test profiles stop or fault a node and move/restore its Pebble directory; deterministic simulation covers storage-write failure boundaries for `N=1..7`; quorum peers remain trusted. | Faulted node is unavailable/readiness-failed during the storage fault; a healthy durable quorum still progresses inside the `0,0,1,1,2,2,3` boundary; after storage is restored it rejoins without unsafe replay or duplicate application. | `TestDSTStorageFailureBoundarySlowQuorumSizesOneThroughSeven`; `env JEPSEN_LOCAL_FAULTS=destructive-storage bash tests/jepsen_local.sh`; `bash tests/chaos_fault_campaign.sh`; `go test ./examples/kv -count=1`. | No target-environment remote claim; external host destructive-storage histories are outside current simulation-scoped release evidence. |
| Bit-level persisted-record corruption | One KV node with checksum-detected or stopped/corrupt data; progress and repair require a Healthy quorum excluding that node plus either an existing verified checkpoint or a fresh checkpoint from a live source replica. | Normal restart fails closed with `epaxos.ErrChecksumMismatch`; `VerifyCheckpoint` rejects checksum, key/ref, applied-marker, timestamp-density, dependency-order, and semantic KV-row mismatches; offline `RepairFromCheckpoint` replaces the whole directory only after verification; `RecoverReplicaFromLiveCheckpoint` checkpoints a live source, verifies live quorum support and the target-owned next-instance floor, then replaces and reopens the corrupt member while healthy peers continue. | `go test ./examples/kv -run 'Test(OpenClusterRejectsBitFlippedPersistedEPaxosRecord|CheckpointRestoreRecoversBitFlippedPersistedEPaxosRecord|CheckpointRepairRecoversBitFlippedPersistedEPaxosRecord|CheckpointRepairRejectsCorruptCheckpointAndLeavesLiveData|VerifyCheckpoint.*|RecoverReplicaFromLiveCheckpoint.*)' -count=1`; `bash tests/chaos_fault_campaign.sh`. | No in-place Pebble/WAL repair, checksum recomputation, corrupt-record deletion, synthesized quorum reconstruction without a verified checkpoint, multi-replica/quorum-loss repair, target-environment drill, or Byzantine/poisoned-peer repair. |
| Configuration-change ordering, pinning, and finite durable replay | Finite local configuration-barrier model, one add-voter transition from three voters to four voters, one remove-voter transition from four voters to three voters, one add-then-remove chain from `{1,2,3}` to `{1,2,3,4}` to `{1,3,4}`, and one finite executed add/remove durable replay slice; old in-flight instances keep their original config. | User commands wait behind observed unexecuted config commands; old and mid-flight instances use their pinned voters/quorums, including a removed voter for instances pinned before that removal; durable replay reconstructs the intermediate historical config for the finite executed add/remove slice, rejects conflicting stored config states, keeps an unexecuted config pending, and removed voters reject local proposals. | `go test ./epaxos -run 'Test.*Config.*|TestRemoveVoterConfChangeKeepsOldInFlightInstancePinned' -count=1`; `/opt/homebrew/opt/openjdk/bin/java -cp /tmp/tla2tools-v1.7.4.jar tlc2.TLC -config tla/EPaxosConfigTransition.cfg tla/EPaxosConfigTransition.tla`; `/opt/homebrew/opt/openjdk/bin/java -cp /tmp/tla2tools-v1.7.4.jar tlc2.TLC -config tla/EPaxosConfigRemoveTransition.cfg tla/EPaxosConfigRemoveTransition.tla`; `/opt/homebrew/opt/openjdk/bin/java -cp /tmp/tla2tools-v1.7.4.jar tlc2.TLC -config tla/EPaxosConfigChainTransition.cfg tla/EPaxosConfigChainTransition.tla`; `/opt/homebrew/opt/openjdk/bin/java -cp /tmp/tla2tools-v1.7.4.jar tlc2.TLC -config tla/EPaxosConfigReplay.cfg tla/EPaxosConfigReplay.tla`; `bash tests/tla_model_check.sh`. | Does not model arbitrary membership-change histories, arbitrary multi-step chains, joint consensus, arbitrary recovery under configuration changes beyond the finite recovery-under-removal/addition rows, arbitrary durable histories, or unbounded histories. |
| Configuration recovery under removal | One finite old-instance recovery-after-removal slice after current config removal; old instance remains pinned to four-voter `Ref.Conf=1` while current config is three voters. | Prepare, accept, and commit recovery messages target the old voters including removed voter 4, prepare/accept quorums require old 3/4 thresholds, and recovered no-op does not emit an application command. | `go test ./epaxos -run 'Test.*Config.*Recovery|Test.*Recovery.*Config|TestRestartAcceptedForeignInstanceSchedulesPrepareRecovery' -count=1`; `TestOldConfigRecoveryUsesPinnedVotersAfterRemoval`; `tla/EPaxosConfigRecovery.cfg`; `bash tests/tla_model_check.sh`. | Does not model arbitrary recovery under configuration changes, joint consensus, message loss, arbitrary membership histories, or unbounded proof. |
| Configuration recovery duplicate/lost response de-duplication | One finite old-instance recovery-after-removal slice with a lost response and a duplicate response before the distinct old quorum response; old instance remains pinned to four-voter `Ref.Conf=1` while current config is three voters. | Lost voter 1 and duplicate voter 3 prepare/accept attempts do not advance the old quorum, the duplicate does not replace the need for distinct removed voter 4, and recovered no-op does not emit an application command. | `go test ./epaxos -run 'TestOldConfigRecoveryUsesPinnedVotersAfterRemoval' -count=1`; `tla/EPaxosConfigRecoveryDedup.cfg`; `bash tests/tla_model_check.sh`. | Does not model retries, timer rebroadcast, arbitrary message loss, arbitrary recovery under configuration changes, joint consensus, or unbounded proof. |
| Configuration recovery under addition | One finite old-instance recovery-after-addition slice after current config adds voter 4; old instance remains pinned to three-voter `Ref.Conf=1` while current config is four voters. | Prepare and accept attempts from added voter 4 do not advance the old quorum; prepare, accept, and commit recovery messages target only old voters `{1,3}` from coordinator 2; old 2/3 quorums are sufficient; recovered no-op does not emit an application command. | `go test ./epaxos -run 'Test.*Config.*Recovery|Test.*Recovery.*Config|TestOldConfigRecoveryUsesPinnedVotersAfterAddition' -count=1`; `TestOldConfigRecoveryUsesPinnedVotersAfterAddition`; `tla/EPaxosConfigAddRecovery.cfg`; `bash tests/tla_model_check.sh`. | Does not model arbitrary recovery under configuration changes, joint consensus, message loss, arbitrary membership histories, or unbounded proof. |
| Configuration transition response de-duplication | One finite normal local-owner old-instance PreAccept/Accept response de-duplication slice after current config removal and one after current config addition; old instances remain pinned to their original `Ref.Conf`, and the restarted local owner vote counts separately from remote responses. | Duplicate remote old-voter responses do not fill the remaining old quorum, added current-voter responses are rejected/ignored for pre-addition instances, the removal slice advances with a second distinct surviving old remote instead of requiring removed voter 4, and accepted cases commit the original user command. | `go test ./epaxos -run 'TestOldConfigTransitionDedup' -count=1`; `TestOldConfigTransitionDedupUsesPinnedVotersAfterRemoval`; `TestOldConfigTransitionDedupUsesPinnedVotersAfterAddition`; `tla/EPaxosConfigTransitionDedup.cfg`; `bash tests/tla_model_check.sh`. | Does not model arbitrary message histories, joint consensus, arbitrary membership histories, arbitrary retry histories, or unbounded proof. |
| Configuration recovery after add/remove chain | One finite mid-chain recovery slice after `{1,2,3}` adds voter 4 and then removes voter 2 to current `{1,3,4}`; the recovered instance remains pinned to `Ref.Conf=2` with voters `{1,2,3,4}`. | Prepare/accept recovery messages target Conf2 peers, dependency vectors keep width 4, local 1 plus current voter 4 does not satisfy the old Conf2 quorum, the modeled removed voter 2 response can supply the third old-quorum vote, and the recovered no-op emits no application command. | `go test ./epaxos -run '^TestOldConfigRecoveryUsesPinnedMidChainVotersAfterAddThenRemove$' -count=1`; `tla/EPaxosConfigChainRecovery.cfg`; `bash tests/tla_model_check.sh`. | Does not model arbitrary recovery histories, arbitrary membership histories, retry histories, joint consensus, message loss, application output, or unbounded proof. |
| AcceptEvidence sender merge and validation | One finite sender-evidence merge/validation slice for a three-slot dependency vector plus focused Go message-validation and codec regressions. | Outbound sender-zero evidence is skipped, same-sender outbound conflicts coalesce by max `Seq` and per-slot max `Deps` into one tuple, distinct-sender evidence appends once, identical duplicate sender evidence remains idempotent, and conflicting duplicate sender evidence is rejected. | `/opt/homebrew/opt/openjdk/bin/java -cp /tmp/tla2tools-v1.7.4.jar tlc2.TLC -config tla/EPaxosAcceptEvidenceMerge.cfg tla/EPaxosAcceptEvidenceMerge.tla`; `go test ./epaxos -run '^(TestSenderPreservingEvidenceValidationAndMergeContracts|TestCodecRejectsMalformedSenderEvidenceWireFrames)$' -count=1`; `bash tests/tla_model_check.sh`. | Does not model arbitrary evidence histories, message delivery, recovery branch choices, complete optimized-recovery parity, or unbounded proof. |
| Service-plane fault containment | HTTP KV node request deadlines, body-size limits, scan bounds, readiness/liveness split, and separate client/peer/admin route planes. | Oversized/invalid requests are rejected before consensus/state mutation; peer route rejects wrong methods before body decode; liveness remains distinct from readiness during storage faults. | `go test -tags kvnode ./examples/kv/cmd/kvnode -count=1`; `tests/ci.sh`; `tests/operations_readiness_audit.sh`. | Does not add authorization, client-certificate authentication, production deployment proof, or target-environment capacity proof. |

## Closed release items

| Item | Current evidence |
| --- | --- |
| Release scope lock | `RELEASE_SCOPE.md`; `tests/release_scope_audit.sh`; `bash tests/release_scope_audit.sh` |
| Review baseline | `RELEASE_SCOPE.md`; `tests/release_scope_audit.sh`; `bash tests/release_scope_audit.sh` |
| Requirement traceability | This table maps each production-readiness requirement to state and evidence; `tests/release_scope_audit.sh` checks required entries. |
| Pin toolchain reproducibly | `tests/toolchain.env`; `tests/toolchain_audit.sh`; `.github/workflows/ci.yml`; `go.mod`; `go.work`; `examples/kv/go.mod`; `tests/tla_model_check.sh`; GitHub Actions are pinned to full 40-character commit SHAs and downloaded jars carry SHA-256 checksums. |
| Missing dependency closure | `epaxos/node.go`; `go test ./epaxos`; `tests/ci.sh` |
| Owner-independent recovery | `epaxos/node.go`; `go test ./epaxos`; `tests/ci.sh` |
| Prepare and TryPreAccept recovery | `epaxos/node.go`; `tla/EPaxosResponses.tla`; `go test ./epaxos`; `bash tests/tla_model_check.sh`; `tests/ci.sh`; finite response model checks committed/accepted/preaccepted/no-op prepare branch priority, the `fast + slow - N` TryPreAccept witness threshold, and current-ballot gating for non-reject AcceptOK evidence. |
| Accept-Deps recovery evidence | `epaxos/types.go`; `epaxos/message.go`; `epaxos/node.go`; `epaxos/codec.go`; `epaxos/checksum.go`; `examples/kv/epaxos_storage.go`; `epaxos/optimized_test.go`; `epaxos/branch_test.go`; `tla/EPaxosOptimizedRecovery.tla`; `tla/EPaxosOptimizedRecovery.cfg`; `tla/EPaxosOptimizedRecoveryFive.cfg`; `tla/EPaxosOptimizedRecoverySeven.cfg`; `go test ./epaxos`; `go test ./examples/kv`; `bash tests/tla_model_check.sh`; Accept/AcceptReply/Commit/PrepareReply carry durable recovery-only aggregate and sender-preserving evidence, non-current-ballot AcceptReply evidence is ignored before counting/merge, read-only evidence queries drive committed stale-dependency TryPreAccept ignore resends or fail-closed slow accept, chosen execution `Seq`/`Deps` remain unchanged, and finite 3-/5-/7-replica optimized-recovery configs exercise the focused model slice. |
| AcceptEvidence sender-merge finite model | `tla/EPaxosAcceptEvidenceMerge.tla`; `tla/EPaxosAcceptEvidenceMerge.cfg`; `tests/tla_model_check.sh`; `MODEL_EQ_REPORT.MD`; `EPAXOS_IMPLEMENTATION_PROOF.md`; `TestSenderPreservingEvidenceValidationAndMergeContracts`; `TestCodecRejectsMalformedSenderEvidenceWireFrames`; direct TLC generated `5/5` states; the finite model keeps sender-zero skip, same-sender `mergeAttrs` coalescing, distinct-sender append, identical duplicate validation acceptance, and conflicting duplicate validation rejection explicit. |
| Implemented fast quorum table | `epaxos/quorum.go`; `EPAXOS.MD`; `MODEL_EQ_REPORT.MD`; `tla/Quorum.tla`; `go test ./epaxos`; `bash tests/tla_model_check.sh`; odd supported cluster sizes use the optimized EPaxos paper quorum with FP-deps-committed prefix evidence, while even sizes retain the conservative quorum because the paper proof assumes odd `N=2F+1`. |
| Paper fast-quorum parity | `epaxos/quorum.go`; `epaxos/node.go`; `epaxos/message.go`; `epaxos/optimized_test.go`; `examples/kv/epaxos_storage.go`; `examples/kv/more_test.go`; `EPAXOS.MD`; `MODEL_EQ_REPORT.MD`; `EPAXOS_IMPLEMENTATION_PROOF.md`; `go test ./epaxos`; `go test ./examples/kv`; odd supported cluster sizes use the paper fast quorum with FP-deps-committed evidence, sender-preserving Accept-Deps evidence, read-only committed stale-dependency evidence search, authorized TryPreAccept ignore resends, and fail-closed slow accept; even sizes intentionally remain conservative because the paper proof assumes odd `N=2F+1`. |
| EPaxos Revisited chain pruning | `epaxos/revisited_test.go`; `go test ./epaxos`; `tests/ci.sh` |
| Durable fast-path marker for EPaxos records | `examples/kv/epaxos_storage.go`; `go test ./examples/kv`; `tests/ci.sh` |
| Deterministic timing boundary | `EPAXOS.MD`; `MODEL_EQ_REPORT.MD`; `go test ./...`; `tests/ci.sh`; non-TOQ protocol timing uses deterministic logical ticks, and explicit TOQ mode uses caller-supplied clock/delay inputs without core-owned goroutines, I/O, or OS wall-clock reads; `TOQOneWayDelay` values must include clock-skew margin. |
| Go/race/coverage gates | `tests/go_coverage.sh`; `tests/ci.sh` |
| Deterministic simulation | `epaxos/sim_test.go`; `epaxos/dst_test.go`; `epaxos/stress_test.go`; `epaxos/config_change_ordering_test.go`; `epaxos/recovery_test.go`; `TestClusterSizesOneThroughSevenCommit`; `TestDSTFailureBoundarySlowQuorumSizesOneThroughSeven`; `TestDSTStorageFailureBoundarySlowQuorumSizesOneThroughSeven`; `TestDSTFailureBoundaryThreeNodeCrashBoundary`; `TestDSTFailureBoundaryFiveNodeMajorityAndMinorityOmission`; `TestDSTLinearizabilityConflictingWritesReadsAcrossPartitionHeal`; `TestDSTStorageFailureRetriesOutstandingReadyExactlyOnceWithHealthyQuorum`; `TestStorageWireRestartUpgradeRollbackSimulationConvergesWithoutDuplicateApply`; `TestTOQThreeNodeFastCommitUsesOptimizedQuorumWithCoveringAttrs`; `TestAcceptRespIgnoresStaleBallotForCurrentAcceptRound`; randomized fixed-seed stress; omission, pause, rollback, owner-failure, partition, storage-failure, and storage/wire restart-rollback simulation coverage. |
| Static/text audit | `tests/audit_repo.sh`; `bash tests/audit_repo.sh`; `tests/ci.sh` |
| Algorithm correspondence | `EPAXOS.MD`; `MODEL_EQ_REPORT.MD`; `tests/tla_model_check.sh`; `tests/ci.sh` |
| Implementation proof document | `EPAXOS_IMPLEMENTATION_PROOF.md`; paper-grounded EPaxos/Revisited obligation map; exact failure-count matrix; property-by-property rationale; verification/non-claim matrix; `bash tests/release_scope_audit.sh` |
| Formal models (finite configured suite) | `tests/tla_model_check.sh`; unique TLC `-metadir` per config; `tla/EPaxos.cfg`; `tla/EPaxosKVConflict.cfg`; `tla/EPaxosThreeReplica.cfg`; `tla/EPaxosResponses.cfg`; `tla/EPaxosResponsesFive.cfg`; `tla/EPaxosRecovery.cfg`; `tla/EPaxosRecoveryFive.cfg`; `tla/EPaxosOptimizedRecovery.cfg`; `tla/EPaxosOptimizedRecoveryFive.cfg`; `tla/EPaxosOptimizedRecoverySeven.cfg`; `tla/EPaxosTryPreAcceptBranches.cfg`; `tla/EPaxosTryPreAcceptBranchesFive.cfg`; `tla/EPaxosTryPreAcceptBranchesSeven.cfg`; `tla/EPaxosEvidenceQuery.cfg`; `tla/EPaxosEvidenceQueryFive.cfg`; `tla/EPaxosEvidenceQuerySeven.cfg`; `tla/EPaxosEvidenceStaleness.cfg`; `tla/EPaxosConfigBarrier.cfg`; `tla/EPaxosConfigTransition.cfg`; `tla/EPaxosConfigRemoveTransition.cfg`; `tla/EPaxosConfigChainTransition.cfg`; `tla/EPaxosRollbackAllocation.cfg`; `tla/EPaxosRevisited.cfg`; `tla/TOQClockDiscipline.cfg`; `tla/ReadyAdvance.cfg`; `tla/ReadyAdvanceCapped.cfg`; `tla/KVTimestampStaleness.cfg`; `tla/KVOmissionRecovery.cfg`; `tla/Quorum.cfg`; `MODEL_EQ_REPORT.MD`; `bash tests/tla_model_check.sh` passed with `EPaxosTryPreAcceptBranches.cfg`, `EPaxosTryPreAcceptBranchesFive.cfg`, and `EPaxosTryPreAcceptBranchesSeven.cfg` each at `17/17`, `EPaxosEvidenceStaleness.cfg` `6/6`, `EPaxosConfigChainTransition.cfg` `299/299`, `EPaxosConfigRemoveTransition.cfg` `70/70`, and `EPaxosRollbackAllocation.cfg` `7/7` generated/distinct states. |
| TryPreAccept retry-timer finite model | `tla/EPaxosTryPreAcceptRetry.tla`; `tla/EPaxosTryPreAcceptRetry.cfg`; `tests/tla_model_check.sh`; `MODEL_EQ_REPORT.MD`; `EPAXOS_IMPLEMENTATION_PROOF.md`; `TestTryPreAcceptTimerDropsStaleEvidenceChecksBeforeRetry`; direct TLC generated `10/10` states. |
| Configuration recovery retry-timer finite model | `tla/EPaxosConfigRecoveryRetry.tla`; `tla/EPaxosConfigRecoveryRetry.cfg`; `tests/tla_model_check.sh`; `MODEL_EQ_REPORT.MD`; `EPAXOS_IMPLEMENTATION_PROOF.md`; `TestOldConfigRecoveryRetryUsesPinnedVotersAfterRemoval`; `TestOldConfigRecoveryRetryUsesPinnedVotersAfterAddition`; direct TLC generated `8/8` states. |
| Configuration transition retry-timer finite model | `tla/EPaxosConfigTransitionRetry.tla`; `tla/EPaxosConfigTransitionRetry.cfg`; `tests/tla_model_check.sh`; `MODEL_EQ_REPORT.MD`; `EPAXOS_IMPLEMENTATION_PROOF.md`; `TestOldConfigTransitionRetryUsesPinnedVotersAfterRemoval`; `TestOldConfigTransitionRetryUsesPinnedVotersAfterAddition`; direct TLC generated `8/8` states. |
| Configuration transition response de-duplication finite model | `tla/EPaxosConfigTransitionDedup.tla`; `tla/EPaxosConfigTransitionDedup.cfg`; `tests/tla_model_check.sh`; `MODEL_EQ_REPORT.MD`; `EPAXOS_IMPLEMENTATION_PROOF.md`; `TestOldConfigTransitionDedupUsesPinnedVotersAfterRemoval`; `TestOldConfigTransitionDedupUsesPinnedVotersAfterAddition`; direct TLC generated `16/16` states; Go restart now re-seeds the persisted local owner vote for normal-owner slow PreAccept/Accept coordination and forces replayed PreAccepted records through slow Accept because fast-quorum dependency coverage is volatile. |
| Configuration chain recovery finite model | `tla/EPaxosConfigChainRecovery.tla`; `tla/EPaxosConfigChainRecovery.cfg`; `tests/tla_model_check.sh`; `MODEL_EQ_REPORT.MD`; `EPAXOS_IMPLEMENTATION_PROOF.md`; `TestOldConfigRecoveryUsesPinnedMidChainVotersAfterAddThenRemove`; direct TLC generated `20/14` states. |
| Local Jepsen restart/transport/storage gates | `tests/jepsen_local.sh`; `jepsen/src/moreconsensus/epaxos_test.clj`; `tests/ci.sh` |
| Local destructive-storage recovery | `tests/jepsen_local.sh`; `jepsen/src/moreconsensus/epaxos_test.clj`; manual evidence: `env JEPSEN_LOCAL_FAULTS=destructive-storage bash tests/jepsen_local.sh` |
| Remote destructive-safety preflight | `tests/jepsen_remote.sh`; `tests/jepsen_remote_preflight_audit.sh`; `jepsen/src/moreconsensus/epaxos_test.clj`; `jepsen/test/moreconsensus/epaxos_test_test.clj`; `bash tests/jepsen_remote_preflight_audit.sh`; remote destructive-storage and wall-clock-skew runs require explicit confirmations, reject broad/unsafe remote directories by default, and support preflight-only validation without touching remote hosts. This is safety preflight evidence, not an external multi-host Jepsen history. |
| Configuration-change ordering | `epaxos/node.go`; `epaxos/config_change_ordering_test.go`; `epaxos/sim_test.go`; `tla/EPaxosConfigBarrier.tla`; `tla/EPaxosConfigTransition.tla`; `tla/EPaxosConfigRemoveTransition.tla`; `tla/EPaxosConfigChainTransition.tla`; `EPAXOS.MD`; `go test ./epaxos`; `bash tests/tla_model_check.sh`; `tests/go_coverage.sh`; finite models cover local `pendingConf` blocking, dependency/sequence barriers for two fixed config refs plus one user command, one add-voter transition where old in-flight instances remain pinned to old voters/quorums while later instances use the new voters/quorum, one remove-voter transition where old in-flight instances can still use old voters/quorums including the removed voter while later instances exclude it, and one add-then-remove chain where old and mid-flight instances remain pinned across two executed config changes. Go guards removed local voters from creating new proposals. They do not model arbitrary membership changes, arbitrary multi-step reconfiguration chains, recovery under config changes, durable replay, or unbounded instance spaces. |
| Ready durable-application failure handling | `epaxos/node.go`; `epaxos/ready_durability_test.go`; `examples/kv/epaxos_storage.go`; `examples/kv/more_test.go`; `EPAXOS.MD`; `tests/go_coverage.sh` |
| Deterministic ProcessAt timing heuristic | `epaxos/node.go`; `epaxos/message.go`; `epaxos/codec.go`; `epaxos/checksum.go`; `epaxos/remaining_test.go`; `examples/kv/cluster.go`; `go test ./epaxos ./examples/kv`; `tests/go_coverage.sh`; separate from explicit TOQ mode. |
| Full EPaxos Revisited TOQ core behavior | `Config.TOQ`; `Config.TOQClock`; `Config.TOQOneWayDelay`; `Config.TOQSyncGroup`; `epaxos/types.go`; `epaxos/message.go`; `epaxos/codec.go`; `epaxos/checksum.go`; `epaxos/node.go`; `epaxos/toq_test.go`; `examples/kv/epaxos_storage.go`; `tla/EPaxosRevisited.tla`; `TestTOQThreeNodeFastCommitUsesOptimizedQuorumWithCoveringAttrs`; `go test ./epaxos`; `go test ./examples/kv`; `bash tests/tla_model_check.sh`; explicit TOQ mode persists `TOQPending`/`ProcessAt`, sends flagged zero-sequence/empty-dependency PreAccepts including immediate `ProcessAt=0`, delays originator assignment until `ProcessAt`, blocks decision while pending, keeps retries explicit, computes receiver attrs at the flagged timestamp, and uses optimized three-node TOQ fast quorum when remote attrs cover local attrs. The embedding application remains responsible for actual synchronized clocks and conservative one-way-delay estimates including skew margin. |
| TOQ clock-discipline finite model | `tla/TOQClockDiscipline.tla`; `tla/TOQClockDiscipline.cfg`; `tests/tla_model_check.sh`; `MODEL_EQ_REPORT.MD`; checks the finite bounded-skew/bounded-delay `ProcessAt` contract used by explicit TOQ mode. The embedding application remains responsible for actual synchronized clocks, one-way-delay measurement, and sync-group construction; configured `TOQOneWayDelay` values must include skew margin. |
| Chaos and fault-injection campaign | `tests/chaos_fault_campaign.sh`; `tests/ci.sh`; `epaxos/sim_test.go`; `epaxos/stress_test.go`; `examples/kv/more_test.go`; `jepsen/src/moreconsensus/epaxos_test.clj`; `bash tests/chaos_fault_campaign.sh` |
| Fuzz stress campaigns | `tests/fuzz_stress_campaign.sh`; `tests/ci.sh`; fixed-seed randomized transport/restart/checksum stress in `epaxos/stress_test.go`; decoder seed corpus and bounded `FuzzDecodeMessage` in `epaxos/pool_ownership_test.go`; default gate command `bash tests/fuzz_stress_campaign.sh` with `MORECONSENSUS_FUZZTIME=5s` unless overridden. |
| Checkpoint-backed bit-level disk-corruption recovery | `examples/kv/backup.go`; `examples/kv/cluster.go`; `examples/kv/epaxos_storage.go`; `examples/kv/more_test.go`; `tests/chaos_fault_campaign.sh`; `TestCheckpointRestoreRecoversBitFlippedPersistedEPaxosRecord`, `TestCheckpointRepairRecoversBitFlippedPersistedEPaxosRecord`, `TestCheckpointRepairRejectsCorruptCheckpointAndLeavesLiveData`, `TestVerifyCheckpointAndRepairFromCheckpointRejectMissingCheckpoint`, `TestVerifyCheckpointRejectsSemanticDataCorruption`, `TestVerifyCheckpointCrashWindowMarkerRules`, `TestRecoverReplicaFromLiveCheckpointRestoresStoppedReplica`, `TestRecoverReplicaFromLiveCheckpointRejectsUnsupportedSource`, and `TestRecoverReplicaFromLiveCheckpointRejectsTargetOwnedFloorMismatch` exercise byte-flipped EPaxos records that fail normal restart, whole-directory offline repair, semantic read-only checkpoint verification for EPaxos records/applied markers/dense timestamped KV rows/dependency order, corrupt/missing/semantically invalid checkpoint rejection without replacing live data, live-source checkpoint replacement with healthy-quorum support, and target-owned floor fail-closed protection against owner-ref reuse. |
| Logical clock skew injection | `epaxos/sim_test.go`; `tests/chaos_fault_campaign.sh`; `go test ./epaxos -run TestUnevenLogicalTickSkewAndBurstConvergesWithoutDuplicates -count=1`; `bash tests/chaos_fault_campaign.sh`; this covers deterministic RawNode tick skew inside the simulation-scoped release evidence. |
| Request deadline budgets | `examples/kv/cmd/kvnode/main.go`; `examples/kv/cmd/kvnode/main_test.go`; `-request-deadline-ms`; `-peer-deadline-ms`; `go test -tags kvnode ./examples/kv/cmd/kvnode`; `tests/ci.sh` |
| Clock pause handling | `epaxos/sim_test.go`; `tests/chaos_fault_campaign.sh`; `go test ./epaxos -run TestPausedClockDoesNotTickOrProcessReadyUntilResume -count=1`; `tests/ci.sh` |
| VM rollback restart catch-up | `epaxos/sim_test.go`; `epaxos/recovery_test.go`; `tla/EPaxosRollbackAllocation.tla`; `tla/EPaxosRollbackAllocation.cfg`; `tests/chaos_fault_campaign.sh`; `go test ./epaxos -run TestRolledBackNodeCatchesUpFromQuorumWithoutDuplicateApply -count=1`; `go test ./epaxos -run TestSimulatorRestoredLocalStorageAdvancesPastLearnedLocalCommit -count=1`; `bash tests/tla_model_check.sh`; Go tests cover quorum catch-up, local `nextInstance` monotonicity after learning own committed refs, learned-before-fresh applied order on all replicas, and no duplicate application; the finite model covers the older-checkpoint/quorum-learn/fresh-allocation rule. |
| Storage/wire restart rollback simulation | `epaxos/sim_test.go`; `TestStorageWireRestartUpgradeRollbackSimulationConvergesWithoutDuplicateApply`; `go test ./epaxos -run Test.*Upgrade.*Rollback -count=1`; deterministic one-node-at-a-time restart plus rollback-from-checkpoint simulation proves quorum progress, catch-up, no duplicate application, and convergence without claiming actual mixed-version binary execution. |
| Local mixed-version binary drill | `tests/kvnode_mixed_version_drill.sh`; old ref `66428ba`, new ref `64cff8d`; `KVNODE_UPGRADE_OLD_REF=66428ba KVNODE_UPGRADE_NEW_REF=64cff8d ... bash tests/kvnode_mixed_version_drill.sh`; local three-node loopback old/new binary SHA-256s differed; each upgraded and rolled-back node served a 204 write followed by latest GET and scan verification from all nodes. Binary rollback restarts the old binary on current data; checkpoint restore remains a separate data-lifecycle fallback. Boundary: this old/new delta is timeout-outcome wording only, so it proves the local drill mechanics for two distinct binaries and does not prove broad protocol/storage compatibility across substantive release deltas. |
| Leap-second-style civil-time boundary | `examples/kv/more_test.go`; `go test ./examples/kv -run 'TestExactStalenessUnderflowDoesNotWrapAcrossCivilClockBoundary|TestBoundedStalenessUsesNumericRecordTimesAcrossRepeatedCivilBoundary|TestStalenessScanKeepsPerKeyHistoryAcrossCivilBoundary' -count=1`; `tests/ci.sh` |
| Crash-idempotent application | `examples/kv/epaxos_storage.go`; `examples/kv/more_test.go`; `go test ./examples/kv -run TestApplyReadyDuplicateCommittedRefIsIdempotent -count=1`; `tests/ci.sh` |
| API separation | `examples/kv/cmd/kvnode/main.go`; `examples/kv/cmd/kvnode/main_test.go`; `-listen`; `-peer-listen`; `-admin-listen`; distinct client/peer/admin muxes; peer `/epaxos/message` is POST-only and rejects non-POST before body decode; `tests/jepsen_local.sh`; `jepsen/src/moreconsensus/epaxos_test.clj`; `jepsen/test/moreconsensus/epaxos_test_test.clj`; `go test -tags kvnode ./examples/kv/cmd/kvnode -run 'Test(APIMuxSeparationRoutesOnlyPlaneEndpoints|HandleMessageRejectsNonPostBeforeReadingBody)$'`; `go test -tags kvnode ./examples/kv/cmd/kvnode`; `cd jepsen && lein test moreconsensus.epaxos-test-test`; `tests/chaos_fault_campaign.sh` |
| Transport security | `examples/kv/cmd/kvnode/main.go`; `examples/kv/cmd/kvnode/main_test.go`; `-tls-cert`; `-tls-key`; `-tls-ca`; `go test -tags kvnode ./examples/kv/cmd/kvnode -run 'Test.*TLS|TestHTTPDeadlineConfiguration' -count=1`; `go test -tags kvnode ./examples/kv/cmd/kvnode`; `tests/ci.sh` |
| Request size limits | `examples/kv/cmd/kvnode/main.go`; `examples/kv/cmd/kvnode/main_test.go`; `-max-client-body-bytes`; `-max-peer-body-bytes`; `-max-admin-body-bytes`; `go test -tags kvnode ./examples/kv/cmd/kvnode -run 'Test.*Body|Test.*TLS|TestPositiveBytes' -count=1`; `go test -tags kvnode ./examples/kv/cmd/kvnode`; `tests/ci.sh` |
| Scan bounds | `examples/kv/cmd/kvnode/main.go`; `examples/kv/cmd/kvnode/main_test.go`; `-max-scan-limit`; `limit`; `prefix`; `start`; `end`; unbounded scans, mixed prefix/range, invalid ranges, and over-limit requests are rejected; `go test -tags kvnode ./examples/kv/cmd/kvnode -run 'TestHandleScanReturnsPrefixRows|TestHandleScanHistoricalReadSelectorsReturnVersionTimes|TestHandleScanRejectsBadQueryAndMethod|TestHandleScanRejectsUnboundedAndInvalidRangeBounds|TestHandleScanRejectsNegativeLimit' -count=1` |
| Point latest-read semantics | `examples/kv/cmd/kvnode/main.go`; `examples/kv/cmd/kvnode/main_test.go`; point `GET /kv/{key}` without a timestamp selector waits for that key's consensus barrier before reading; `go test -tags kvnode ./examples/kv/cmd/kvnode -run TestHandleKVLatestReadWithoutSelectorStillWaitsForConsensusBarrier -count=1` |
| Scan latest-read semantics | `examples/kv/cmd/kvnode/main.go`; `examples/kv/cmd/kvnode/main_test.go`; default latest `GET /scan` requests without timestamp selectors and without explicit `barrier=` propose a scan-wide consensus barrier conflict key, while HTTP KV writes/transactions/deletes carry the same conflict key; historical selectors still avoid consensus progress; `go test -tags kvnode ./examples/kv/cmd/kvnode -run TestHandleScanLatestReadWithoutSelectorOrExplicitBarrierWaitsForScanBarrier -count=1`; `go test -tags kvnode ./examples/kv/cmd/kvnode -count=1` |
| Binary value fidelity | `examples/kv/cmd/kvnode/main.go`; `examples/kv/cmd/kvnode/main_test.go`; point PUT/GET remain raw bytes; transaction JSON accepts `value_b64` and rejects ambiguous `value`+`value_b64`; scan JSON emits `value_b64` for non-UTF-8 values; `go test -tags kvnode ./examples/kv/cmd/kvnode -run 'TestHandleTxnBase64ValuePreservesNonUTF8BytesThroughGetAndScan|TestHandleTxnRejectsMalformedJSONAndInvalidRequests' -count=1`; `go test -tags kvnode ./examples/kv/cmd/kvnode -count=1` |
| Health readiness split | `examples/kv/cmd/kvnode/main.go`; `examples/kv/cmd/kvnode/main_test.go`; admin `/livez` stays live while `/readyz` returns unavailable during storage fault; `/health` remains a liveness-compatible endpoint; `go test -tags kvnode ./examples/kv/cmd/kvnode -run 'TestAdminLivenessAndReadinessSplit|TestAPIMuxSeparationRoutesOnlyPlaneEndpoints' -count=1`; `go test -tags kvnode ./examples/kv/cmd/kvnode -count=1` |
| Semantic scan checking | `jepsen/src/moreconsensus/epaxos_test.clj`; `jepsen/test/moreconsensus/epaxos_test_test.clj`; advanced scan checker reconstructs per-key scan histories with Jepsen operation intervals, treats same-key pre-scan overlapping mutations as multiple possible states, rejects wrong values, missing eligible rows, and deleted rows when no same-key overlap can justify them, and verifies unrelated overlaps do not suppress required-row checks; `cd jepsen && lein test moreconsensus.epaxos-test-test` |
| Observability | `examples/kv/cmd/kvnode/main.go`; `examples/kv/cmd/kvnode/main_test.go`; admin `/metrics` exposes low-cardinality storage-fault, transport-drop, EPaxos instance/executed, and send-queue gauges without new dependencies; `go test -tags kvnode ./examples/kv/cmd/kvnode -run 'TestHandleMetricsReportsLowCardinalityAdminState|TestAPIMuxSeparationRoutesOnlyPlaneEndpoints' -count=1`; `go test -tags kvnode ./examples/kv/cmd/kvnode -count=1` |
| Production limits documentation | `README.md` support boundary names the production library surface, example-service boundary, cluster-size limits, timing limits, quorum non-claims, finite-model limits, HTTP body/scan limits, binary-value routes, latest-read scope, checksum fail-fast behavior, and checkpoint-backed corruption-recovery limits; `EPAXOS.MD`; `MODEL_EQ_REPORT.MD`; `bash tests/audit_repo.sh`; `bash tests/release_scope_audit.sh` |
| API contracts documentation | `README.md` API contracts plus `EPAXOS.MD` document `RawNode`/`Ready`/`Advance`, persistence-before-send/apply, idempotent committed application, checksum errors, transport codec ownership, exact-byte conflict keys, zero-copy proposal ownership, scan consistency scope, and binary-value behavior; `bash tests/audit_repo.sh`; `bash tests/release_scope_audit.sh` |
| Operations readiness artifact audit | Example/operator artifacts exist and are audit-gated, not production-proven: `deploy/systemd/kvnode@.service`; `deploy/systemd/kvnode.env.example`; `examples/kv/cmd/kvcheckpoint`; `examples/kv/cmd/kvcheckpoint/main_test.go`; `tests/kvnode_systemd_manifest_audit.sh`; `tests/kvnode_incident_tabletop_drill.sh`; `tests/kvnode_local_capacity_drill.sh`; `tests/kvnode_local_runner.go`; `docs/operations/kvnode-data-lifecycle-incident-runbook.md`; `docs/operations/kvnode-upgrade-rollback.md`; `tests/kvnode_capacity_envelope.sh`; `tests/operations_readiness_audit.sh`; `bash tests/operations_readiness_audit.sh`; `go run -tags kvnode_local_runner ./tests/kvnode_local_runner.go --help`; `tests/ci.sh` now runs the operations readiness audit before release-scope and repository audits. |
| Evidence bundle and go/no-go workflow | `release/EPAXOS_READINESS_EVIDENCE.md`; `tests/go_no_go_workflow.sh`; `tests/ci.sh`; `bash tests/go_no_go_workflow.sh` returns the current `No-go.` decision and lists open release items; `bash tests/release_scope_audit.sh` checks the evidence/workflow paths. |

TryPreAccept message-path coverage note: `tests/tla_model_check.sh` now runs `tla/EPaxosTryPreAcceptMessagePath.cfg`, `tla/EPaxosTryPreAcceptMessagePathFive.cfg`, and `tla/EPaxosTryPreAcceptMessagePathSeven.cfg`; each finite 3/5/7 config covers follower `MsgTryPreAccept` commit-only, stale/conflict reject, duplicate matching re-ack without durable rewrite, fresh durable ack, and coordinator `MsgTryPreAcceptResp` stale restart, older/duplicate OK ignore, first OK below quorum, pre-seeded quorum immediate accept, and OK slow-quorum accept.

Evidence staleness note: `tests/tla_model_check.sh` now runs `tla/EPaxosEvidenceStaleness.cfg`; the finite three-voter request-scoping model checks that an older-ballot `MsgEvidenceResp` is dropped before sender-record insertion, same-ballot duplicate responses keep the first stale/empty record, stale duplicate fresh evidence cannot authorize a TryPreAccept ignore resend, all recorded empty remotes fail closed to slow Accept, and generated `6/6` states. Go regression `TestEvidenceStaleDuplicateCommittedTupleFallsBackToSlowAccept` checks the matching old-ballot drop, same-ballot first-response-wins, slow-accept fallback, and stale-check cleanup path.
TryPreAccept retry-timer note: `tests/tla_model_check.sh` now runs `tla/EPaxosTryPreAcceptRetry.cfg`; the finite logical-timer model checks that same-target current-ballot pending evidence fails closed to slow Accept before retry, same-target stale-ballot evidence is deleted before retry, unrelated-target evidence is retained, and normal/ignore-marker retry rebroadcasts have no durable/application effects. Go regression `TestTryPreAcceptTimerDropsStaleEvidenceChecksBeforeRetry` checks stale cleanup, unrelated preservation, immediate retry rebroadcast, and deterministic retry rescheduling.

TryConflict force/defer quorum note: `tests/tla_model_check.sh` now runs `tla/EPaxosTryConflictForce.cfg`, `tla/EPaxosTryConflictForceFive.cfg`, and `tla/EPaxosTryConflictForceSeven.cfg`; the finite 3/5/7 model checks `leaderMustBeInCandidateFastQuorum` arithmetic for conflict-leader force, deferred-cycle force, optional-leader deferral, existing-dependency guard, and blocker-recovery deferral.
Config transition retry note: `tests/tla_model_check.sh` now runs `tla/EPaxosConfigTransitionRetry.cfg`; the finite model checks normal local-owner old-config PreAccept/Accept timer rebroadcast after both voter removal and voter addition, keeps old dependency widths, includes removed old voters for old removal instances, excludes added current voters for old pre-addition instances, and generated `8/8` states. Go regressions `TestOldConfigTransitionRetryUsesPinnedVotersAfterRemoval` and `TestOldConfigTransitionRetryUsesPinnedVotersAfterAddition` check the matching removal and addition retry paths.
Config transition de-dup note: `tests/tla_model_check.sh` now runs `tla/EPaxosConfigTransitionDedup.cfg`; the finite model records remote PreAccept/Accept responses separately from the counted local owner vote, checks duplicate old-voter responses stay below old quorum, checks added current-voter responses are excluded for old pre-addition instances, advances only when the modeled second distinct old remote response reaches old quorum, and generated `16/16` states. Go regressions `TestOldConfigTransitionDedupUsesPinnedVotersAfterRemoval` and `TestOldConfigTransitionDedupUsesPinnedVotersAfterAddition` check the matching removal and addition response de-duplication paths, including duplicate voter 2 in the removal slice, added voter 4 in the addition slice, old-config targets, old dependency widths, and original user commands.

Config replay note: `tests/tla_model_check.sh` now runs `tla/EPaxosConfigReplay.cfg`; the finite model checks durable restart replay from the caller's initial config plus the last durable config state through executed add/remove config records, reconstructs the intermediate historical config, preserves old and mid-flight dependency domains, keeps an unexecuted config pending, rejects removed-voter local proposals, and generated `6/6` states. Go coverage also rejects stored historical config states whose voters conflict with the executed add/remove replay result, and verifies TOQ default sync group uses replayed current voters while stale explicit sync groups fail. The remaining durable-replay non-claim means arbitrary durable histories, recovery under reconfiguration, joint consensus, and unbounded membership proof.


Config recovery note: `tests/tla_model_check.sh` now runs `tla/EPaxosConfigRecovery.cfg`; the finite staged model checks recovery for one old four-voter instance after the current config removes voter 4, requires the old slow quorum instead of the smaller current quorum, counts the removed voter only for that old pinned instance, rejects removed-voter new-config proposals, and generated `44/30` states. Go regression `TestOldConfigRecoveryUsesPinnedVotersAfterRemoval` checks the matching prepare/accept/commit broadcasts include removed voter 4, require old 3/4 quorums, and emit no application command for the recovered no-op.

Config recovery de-dup note: `tests/tla_model_check.sh` now runs `tla/EPaxosConfigRecoveryDedup.cfg`; the finite staged model checks one lost prepare/accept response and one duplicate prepare/accept response before the distinct removed-voter response, keeps the old quorum below 3/4 until removed voter 4 answers, proves lost/duplicate responses do not count as distinct old-quorum voters, and generated `11/11` states. Go regression `TestOldConfigRecoveryUsesPinnedVotersAfterRemoval` checks duplicate voter 3 prepare/accept responses do not advance Ready before voter 4 answers, while the recovered no-op still emits no application command.
Config recovery retry note: `tests/tla_model_check.sh` now runs `tla/EPaxosConfigRecoveryRetry.cfg`; the finite model checks old-config prepare/accept timer rebroadcast after both voter removal and voter addition, keeps old dependency widths, includes removed old voters for old removal instances, excludes added current voters for old pre-addition instances, and generated `8/8` states. Go regressions `TestOldConfigRecoveryRetryUsesPinnedVotersAfterRemoval` and `TestOldConfigRecoveryRetryUsesPinnedVotersAfterAddition` check the matching removal and addition retry paths.


Config add-recovery note: `tests/tla_model_check.sh` now runs `tla/EPaxosConfigAddRecovery.cfg`; the finite staged model checks recovery for one old three-voter instance after the current config adds voter 4, forces added-voter prepare/accept attempts to leave old quorum sets unchanged, proves the old 2/3 quorum is sufficient instead of the larger current 3/4 quorum, executes the recovered no-op, and generated `15/15` states. Go regression `TestOldConfigRecoveryUsesPinnedVotersAfterAddition` checks the matching prepare/accept attempts from added voter 4 do not advance Ready, old-voter response does advance recovery, broadcasts exclude voter 4, and no application command is emitted for the recovered no-op.
Config chain recovery note: `tests/tla_model_check.sh` now runs `tla/EPaxosConfigChainRecovery.cfg`; the finite model checks one mid-chain Conf2 recovery after `{1,2,3}` adds voter 4 and then removes voter 2, keeps prepare/accept below quorum when only current-conf quorum responses are present, accepts removed voter 2 as the modeled third old-quorum vote, rejects removed-voter current-config proposals, and generated `20/14` states. Go regression `TestOldConfigRecoveryUsesPinnedMidChainVotersAfterAddThenRemove` checks the matching prepare/accept broadcasts to Conf2 peers `{2,3,4}`, width-4 dependencies, current-quorum-insufficient intermediate states, final old-quorum commit, and no application command for the recovered no-op.


## Open release items

| Item | Current state |
| --- | --- |
| Broader formal model coverage | Finite configured TLC models are closed above, including bounded prepare branch-priority/try-witness coverage, finite 3-, 5-, and 7-replica Accept-Deps optimized-recovery evidence coverage, finite 3-, 5-, and 7-replica abstract TryPreAccept response branch-slice coverage, finite 3-, 5-, and 7-replica TryPreAccept message-path coverage, finite 3-, 5-, and 7-replica committed-conflict evidence-query guard/fail-closed coverage, one finite three-voter committed-conflict evidence-staleness request-scoping slice (`tla/EPaxosEvidenceStaleness.cfg` generated `6/6` states), finite 3-, 5-, and 7-replica uncommitted-conflict force/defer quorum coverage, finite configuration-barrier/add/remove/chain pinning coverage, one finite normal configuration-transition retry-timer slice (`tla/EPaxosConfigTransitionRetry.cfg` generated `8/8` states), one finite normal configuration-transition response de-duplication slice (`tla/EPaxosConfigTransitionDedup.cfg` generated `16/16` states), one finite durable configuration replay slice, finite config recovery-after-removal, recovery-after-addition, lost/duplicate response de-duplication, and recovery retry-timer slices (`tla/EPaxosConfigRecovery.cfg` generated `44/30` states, `tla/EPaxosConfigAddRecovery.cfg` generated `15/15` states, `tla/EPaxosConfigRecoveryDedup.cfg` generated `11/11` states, and `tla/EPaxosConfigRecoveryRetry.cfg` generated `8/8` states), a finite rollback-allocation next-instance/skip/apply-order check, and a finite `TOQClockDiscipline.tla` bounded-skew/bounded-delay contract. Remaining open: arbitrary/general recovery under configuration changes beyond the finite recovery slices, arbitrary membership histories, arbitrary durable histories, joint consensus, arbitrary message loss and retry/rebroadcast behavior, complete optimized-recovery branch parity, unbounded proofs, external target proof, synchronized-clock implementation, one-way-delay measurement, and operational clock-discipline proof. |
| Deployment manifest | Example systemd artifacts now exist (`deploy/systemd/kvnode@.service`, `deploy/systemd/kvnode.env.example`) plus `tests/kvnode_systemd_manifest_audit.sh`, which renders the example EnvironmentFile into the `ExecStart` contract and keeps `systemd-analyze verify` opt-in via `KVNODE_SYSTEMD_ANALYZE=yes`; these artifacts are checked by `tests/operations_readiness_audit.sh`. A reviewed and exercised target deployment under systemd/container/orchestration remains open before this can be a production manifest claim. |
| Data lifecycle | Local destructive-storage remove/restore evidence exists, the KV example has exercised Pebble checkpoint/whole-directory restore plus offline and live-source checkpoint-backed repair tests for checksum-detected bit-level corruption, `examples/kv/cmd/kvcheckpoint` provides a maintained offline checkpoint/verify/verified-restore/repair helper, `TestRestoreRejectsCorruptCheckpointWithoutReplacingLiveData` verifies restore fails closed before replacement, and `docs/operations/kvnode-data-lifecycle-incident-runbook.md` documents checkpoint, verification, repair, restore, checksum-mismatch, and evidence-capture procedures. A reviewed operator backup/restore/disaster-recovery drill in the target environment remains open. |
| Capacity envelope | `tests/kvnode_capacity_envelope.sh` is an opt-in bounded harness for throughput, latency, memory RSS, disk growth, queue depth, value size, scan limit, and peer-count samples; `tests/kvnode_local_capacity_drill.sh` starts a disposable three-node loopback cluster and runs that harness against all three client/admin listeners with PIDs and data dirs; `tests/kvnode_local_runner.go` is a custom Go runner that starts the same local-only three-node loopback shape and records bounded write/read/scan latency plus admin metric samples. `bash -n tests/kvnode_capacity_envelope.sh`, `bash tests/kvnode_capacity_envelope.sh --help`, `bash tests/kvnode_local_capacity_drill.sh --help`, `go run -tags kvnode_local_runner ./tests/kvnode_local_runner.go --help`, and `tests/operations_readiness_audit.sh` pass. Local loopback samples have passed, including the earlier single-node workstation sample, a three-node local wrapper sample with 5 ops per value-size phase, 64/1024-byte values, scan limits 1/8, and peer_count=3, and a custom Go runner sample with `KVNODE_GO_RUNNER_OPS_PER_PHASE=2`, `KVNODE_GO_RUNNER_VALUE_BYTES=16`, `KVNODE_GO_RUNNER_SCAN_LIMITS=1`, and `status=local-go-runner-only`. This is workstation harness evidence only; measured target-environment capacity results remain open. |
| Incident readiness | `docs/operations/kvnode-data-lifecycle-incident-runbook.md` now covers storage failure, network partition, peer compromise, replay/checksum suspicion, and recovery stalls, with evidence-capture steps and non-claims; `tests/kvnode_incident_tabletop_drill.sh` locally rehearses the storage-failure and network-partition test-fault branches on a disposable loopback cluster; `tests/kvnode_local_runner.go` also locally exercised `/faults/storage`, `/faults/transport`, `/readyz`, `/metrics`, and post-clear canaries with `status=local-go-runner-only`; `tests/operations_readiness_audit.sh` checks those artifacts. Operator-reviewed target-environment tabletop or live drill evidence remains open. |

TryPreAccept response branch-slice note: `tla/EPaxosTryPreAcceptBranches.tla` is only a finite abstract scenario/stage model for stale restart, committed evidence ignore/fail-closed, direct/forced accept, one uncommitted deferral with duplicate suppression, and OK slow-quorum accept. Its only quorum detail is `okVotes >= SlowQuorum`, it runs for 3/5/7, and complete optimized-recovery branch parity beyond this finite slice remains open.

TryPreAccept message-path limit: `tla/EPaxosTryPreAcceptMessagePath.tla` is finite 3/5/7 request/response path coverage, not a full network, evidence-query-internal, complete optimized-recovery branch-parity, or unbounded recovery proof.
Evidence staleness limit: `tla/EPaxosEvidenceStaleness.tla` is a finite three-voter request-scoping slice for committed-conflict evidence responses; it does not model retries, arbitrary network delivery, message loss, complete optimized-recovery branch parity, or unbounded recovery proof.
AcceptEvidence merge limit: `tla/EPaxosAcceptEvidenceMerge.tla` is a finite sender-evidence merge/validation slice for one three-slot dependency vector; it does not model arbitrary evidence histories, message delivery, recovery branch choices, complete optimized-recovery branch parity, or unbounded proof.
TryPreAccept retry-timer limit: `tla/EPaxosTryPreAcceptRetry.tla` is a finite logical-timer slice for one TryPreAccept retry step after pending-evidence checks; it does not model arbitrary network delivery, message loss, complete optimized-recovery branch parity, recovery under configuration changes, or unbounded recovery proof.
TryConflict force/defer limit: `tla/EPaxosTryConflictForce.tla` is finite quorum-arithmetic coverage for one uncommitted-conflict force/defer decision, not arbitrary recovery history, full message delivery, complete optimized-recovery branch parity, or unbounded proof.
Config transition retry limit: `tla/EPaxosConfigTransitionRetry.tla` is a finite logical-timer slice for normal local-owner old-config PreAccept/Accept retry rebroadcast after one removal and one addition scenario; it does not model arbitrary retry histories, joint consensus, message loss, arbitrary membership histories, or unbounded proof.
Config transition de-dup limit: `tla/EPaxosConfigTransitionDedup.tla` is a finite response de-duplication slice for normal local-owner old PreAccept/Accept instances after one removal and one addition scenario; it does not model arbitrary message histories, joint consensus, arbitrary membership histories, arbitrary retry histories, or unbounded proof.
Config replay limit: `tla/EPaxosConfigReplay.tla` is a finite restart replay slice for one add-then-remove durable config history; arbitrary durable histories, recovery under configuration changes, joint consensus, and unbounded membership changes remain open.
Config recovery limit: `tla/EPaxosConfigRecovery.tla` is a finite staged recovery-under-removal slice for one old pinned instance, not arbitrary recovery under configuration changes, joint consensus, message loss, arbitrary membership histories, or unbounded recovery proof.
Config recovery de-dup limit: `tla/EPaxosConfigRecoveryDedup.tla` is a finite staged lost+duplicate response de-duplication slice for one old pinned instance; it does not model retries, timer rebroadcast, arbitrary message loss, arbitrary recovery under configuration changes, joint consensus, arbitrary membership histories, or unbounded recovery proof.
Config recovery retry limit: `tla/EPaxosConfigRecoveryRetry.tla` is a finite logical-timer slice for old-config prepare/accept retry rebroadcast after one removal and one addition scenario; it does not model arbitrary recovery histories, arbitrary retry histories, joint consensus, message loss, arbitrary membership histories, or unbounded proof.
Config add-recovery limit: `tla/EPaxosConfigAddRecovery.tla` is a finite staged recovery-after-addition slice for one old pinned instance, not arbitrary recovery under configuration changes, joint consensus, message loss, arbitrary membership histories, or unbounded recovery proof.
Config chain recovery limit: `tla/EPaxosConfigChainRecovery.tla` is a finite mid-chain recovery slice for one add-then-remove history and one chosen response order; it does not model arbitrary recovery histories, arbitrary membership histories, retry histories, joint consensus, message loss, application output, or unbounded proof.


## Review baseline

The review baseline is the current repository state after the EPaxos recovery, Revisited chain-pruning, explicit TOQ core behavior, deterministic `ProcessAt` timing, persistence, local fault-injection campaign, Jepsen checker, and documentation updates verified by `tests/ci.sh`. Reviewers should begin with:

- `EPAXOS.MD` for implemented protocol behavior and explicit TOQ embedder obligations.
- `MODEL_EQ_REPORT.MD` for model correspondence and verification scope.
- `EPAXOS_IMPLEMENTATION_PROOF.md` for the implemented algorithm, property claims, failure-count boundaries, and proof/evidence rationale.
- `RELEASE_SCOPE.md` for closed items, open items, and non-claims.
- `tests/ci.sh` for the currently wired gate.
- `.github/workflows/ci.yml` for the hosted gate entry point.

## Non-claims

- Paper fast-quorum implementation parity is closed for odd supported sizes in Go with FP-deps-committed fast commits, sender-preserving committed stale-dependency evidence search/resend-ignore, targeted leader-in-fast-quorum/deferred-cycle handling, fail-closed slow accept, finite `tla/EPaxosEvidenceQuery.tla` coverage of the read-only evidence-query path, and finite `tla/EPaxosEvidenceStaleness.tla` coverage of evidence response request scoping. Even sizes remain conservative by design, and unbounded proof plus complete optimized-recovery branch parity remain non-claims.
- Finite `tla/EPaxosTryPreAcceptRetry.tla` coverage now checks one TryPreAccept logical retry-timer slice after pending-evidence checks. Arbitrary network delivery, message loss, recovery under configuration changes, complete optimized-recovery branch parity, and unbounded proof remain non-claims.
- Finite `tla/EPaxosConfigRecoveryRetry.tla` coverage now checks one old-config recovery retry-timer slice after removal and addition. Arbitrary recovery histories, arbitrary retry histories, joint consensus, message loss, arbitrary membership histories, and unbounded proof remain non-claims.
- Finite `tla/EPaxosConfigTransitionRetry.tla` coverage now checks one normal old-config transition retry-timer slice after removal and addition. Arbitrary retry histories, joint consensus, message loss, arbitrary membership histories, and unbounded proof remain non-claims.
- Explicit TOQ core behavior assumes the embedding application supplies synchronized clock reads and one-way-delay estimates that include clock-skew/sync margin; `tla/TOQClockDiscipline.tla` checks only a finite bounded-skew/bounded-delay `ProcessAt` contract, and the core does not synchronize clocks, measure one-way delay, or prove an operational clock-discipline implementation.
- Local Jepsen passing and deterministic simulation do not claim target-environment external histories.
- The local mixed-version drill used distinct old/new binaries whose source delta was limited to proposal-timeout outcome wording; it proves the drill harness mechanics and local rollback procedure for that narrow delta, not broad mixed-version compatibility across substantive protocol or storage changes.
- The chaos campaign is a named local CI gate; it closes only simulation/local-loopback checkpoint-backed single-node corruption recovery. It does not close in-place Pebble/WAL repair, checksum recomputation, corrupt-record deletion, synthesized reconstruction without a verified checkpoint, production deployment proof, target capacity proof, or incident drill evidence listed as open.
- Request deadline budgets use explicit Go HTTP and handler deadlines; they are not OS-clock mutation evidence.
- Clock pause handling covers deterministic logical RawNode tick pause/resume, not OS scheduler suspension or wall-clock pause.
- VM rollback restart catch-up covers an in-memory RawNode/storage rollback and quorum catch-up without duplicate apply plus a finite TLA rollback-allocation check for one learned own ref and one defensive skip state; it does not close in-place bit-level disk repair, synthesized reconstruction without a verified checkpoint, multi-replica/quorum-loss recovery, unbounded rollback histories, target-environment deployment proof, or checkpoint-restore data-lifecycle drills.
- API separation covers distinct client, peer-replication, and administrative HTTP listener/route planes; peer replication is POST-only and rejects other methods before body decode; TLS evidence is tracked separately under Transport security and does not add authentication or authorization.
- Transport security covers optional TLS listeners plus CA-configured peer HTTPS client verification; it does not implement client-certificate authentication, HTTP authentication, or authorization.
- Request size limits cover HTTP request body bytes on client, peer, and administrative planes; they do not bound scan result cardinality or keyspace traversal.
- Scan latest-read semantics are closed for the HTTP KV node's own write path by a shared scan-barrier conflict key; this does not claim protocol-native range/prefix conflict predicates for arbitrary EPaxos applications.
- Binary value fidelity covers raw point values plus `value_b64` for transaction/scan JSON; legacy `value` JSON fields remain text-compatible and are not a binary transport for invalid UTF-8.
- This lock records open work; it does not implement those open items.
